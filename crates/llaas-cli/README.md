# llaas-cli

Command-line client for LLAAS.

The root `llaas` binary remains the behavior-preserving implementation for now. This crate will become the GraphQL client CLI during the refactor.

## Verification

```sh
cargo check -p llaas-cli
```